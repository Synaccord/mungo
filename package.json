{
  "name": "mung",
  "version": "1.0.11",
  "description": "Mung ===",
  "main": "dist/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --compilers js:babel/register test/convert.js test/validate.js test/schema.js test/document.js",
    "transpile": "babel app --modules common --stage 0 --optional runtime --out-dir dist",
    "hot-transpile": "rm -rf dist; babel app --modules common --stage 0 --optional runtime --watch --out-dir dist",
    "prepublish": "npm run transpile"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^2.3.3",
    "should": "^7.1.0"
  },
  "dependencies": {
    "babel": "^5.8.23",
    "babel-runtime": "^5.8.25",
    "colors": "^1.1.2",
    "mongodb": "^2.0.45"
  }
}
